# 📚 База данных стихов - Документация

## Что было добавлено

### 1. Структура данных (папка `/data/`)

#### `poems.json` - 30 стихотворений
Содержит коллекцию из 30 стихотворений с полями:
- `id` - уникальный идентификатор
- `title` - название стихотворения
- `author` - имя автора
- `authorId` - ID автора для связи с poets.json
- `year` - год написания
- `text` - полный текст стихотворения
- `tags` - массив тегов (жанр, тема)
- `mood` - настроение стихотворения

**Авторы в коллекции:**
- Александр Пушкин (5 стихов)
- Сергей Есенин (6 стихов)
- Михаил Лермонтов (5 стихов)
- Марина Цветаева (4 стиха)
- Анна Ахматова (3 стиха)
- Владимир Маяковский (3 стиха)
- Афанасий Фет (2 стиха)
- Константин Симонов (1 стих)
- Федор Тютчев (2 стиха)

#### `poets.json` - 9 поэтов
Полная информация о каждом поэте:
- Биография
- Годы жизни
- Период творчества
- Стиль
- Известные произведения
- Путь к изображению

#### `facts.json` - 20 фактов
Интересные факты о поэзии и поэтах для страницы "Удиви меня"

---

### 2. PoemsManager API (`/scripts/poems-manager.js`)

Менеджер для работы с данными. Автоматически загружается при загрузке страницы.

#### Основные методы:

**Загрузка данных:**
```javascript
await poemsManager.loadData(); // Вызывается автоматически
```

**Работа со стихами:**
```javascript
poemsManager.getAllPoems();                    // Все стихи
poemsManager.getPoemById(5);                   // Стих с ID=5
poemsManager.getPoemsByAuthor('pushkin');      // Все стихи Пушкина
poemsManager.getRandomPoem();                  // Случайное стихотворение
```

**Поиск и фильтрация:**
```javascript
poemsManager.searchPoems('любовь');            // Поиск по тексту
poemsManager.filterByTags(['любовь', 'грусть']); // Фильтр по тегам
poemsManager.filterByMood('melancholic');      // Фильтр по настроению
poemsManager.sortPoems(poems, 'year');         // Сортировка
```

**Работа с поэтами:**
```javascript
poemsManager.getAllPoets();                    // Все поэты
poemsManager.getPoetById('esenin');           // Поэт по ID
poemsManager.getAuthorStats('pushkin');       // Статистика по автору
```

**Работа с фактами:**
```javascript
poemsManager.getAllFacts();                    // Все факты
poemsManager.getRandomFact();                  // Случайный факт
```

**Утилиты:**
```javascript
poemsManager.getAllTags();                     // Список всех тегов
poemsManager.getAllMoods();                    // Список всех настроений
```

#### События:
```javascript
// Событие после загрузки данных
document.addEventListener('poemsDataLoaded', (event) => {
    console.log('Загружено стихов:', event.detail.poems);
    // Ваш код здесь
});
```

---

### 3. Новые страницы

#### `index.html` - обновлена
✅ Добавлена кнопка "Вдохновиться" - показывает случайное стихотворение
✅ Модальное окно для отображения стихов
✅ Подключен poems-manager.js

#### `pages/poems.html` - новая страница
Полноценный каталог стихов с возможностями:
- 🔍 Поиск по названию, автору, тексту
- 🏷️ Фильтрация по тегам (кликабельные кнопки)
- 😊 Фильтрация по настроению
- 🔄 Сортировка (по названию, автору, году)
- 📖 Модальное окно для чтения полного текста
- 📊 Счетчик стихов и поэтов

**Навигация обновлена:**
- Добавлена ссылка "Стихи" в меню

---

## Как использовать

### 1. Подключение на странице
```html
<script src="../scripts/poems-manager.js"></script>
```

### 2. Использование в коде
```javascript
// Дождаться загрузки данных
document.addEventListener('poemsDataLoaded', () => {
    // Теперь можно использовать poemsManager
    const randomPoem = poemsManager.getRandomPoem();
    console.log(randomPoem.title);
});
```

### 3. Пример: показать случайное стихотворение
```javascript
function showRandomPoem() {
    const poem = poemsManager.getRandomPoem();
    alert(`${poem.title}\n\n${poem.author}\n\n${poem.text}`);
}

document.addEventListener('poemsDataLoaded', () => {
    document.getElementById('myBtn').addEventListener('click', showRandomPoem);
});
```

---

## Что можно добавить дальше

### Приоритет 1 (для курсовой):
- [ ] Страница детального просмотра поэта (`poet-detail.html?id=pushkin`)
- [ ] Локальное избранное через localStorage
- [ ] Страница "Мои избранные"
- [ ] Счетчик избранного в header

### Приоритет 2 (расширенное):
- [ ] История чтения (localStorage)
- [ ] Статистика пользователя
- [ ] Генератор цитат
- [ ] Режим чтения с настройками шрифта
- [ ] Экспорт избранного в PDF

### Приоритет 3 (продвинутое):
- [ ] Аудио-стихи (Web Speech API)
- [ ] Анализ стихотворений (облако слов, статистика)
- [ ] Викторина "Угадай автора"
- [ ] Стихотворение дня

---

## Структура файлов (обновленная)

```
pr1/
├── data/
│   ├── poems.json          ✅ НОВОЕ - 30 стихов
│   ├── poets.json          ✅ НОВОЕ - 9 поэтов
│   └── facts.json          ✅ НОВОЕ - 20 фактов
├── scripts/
│   ├── poems-manager.js    ✅ НОВОЕ - API для работы с данными
│   ├── theme.js
│   ├── load-components.js  ✅ ОБНОВЛЕНО - новая навигация
│   └── ...
├── pages/
│   ├── poems.html          ✅ НОВОЕ - каталог стихов
│   ├── poets.html
│   ├── surprise.html
│   └── nothing.html
├── includes/
│   ├── header.html         ✅ ОБНОВЛЕНО - добавлена ссылка "Стихи"
│   └── footer.html
├── styles/
│   └── styles.css          ✅ ОБНОВЛЕНО - стили для модального окна
└── index.html              ✅ ОБНОВЛЕНО - кнопка "Вдохновиться"
```

---

## Демонстрация функционала

### Запуск локально:
1. Откройте проект через Live Server (расширение VS Code)
2. Перейдите на главную страницу
3. Нажмите "Вдохновиться" - откроется случайное стихотворение
4. Перейдите в "Стихи" - увидите каталог с поиском и фильтрами

### Что показать в курсовой:
1. **Главная страница**: кнопка "Вдохновиться" с модальным окном
2. **Каталог стихов**: поиск, фильтры, сортировка
3. **Открыть консоль**: показать `poemsManager` и методы
4. **Код**: показать структуру JSON и PoemsManager API

---

## Технические детали

- ✅ Никаких внешних зависимостей для работы с данными
- ✅ Полностью клиентское решение (без бэкенда)
- ✅ Данные загружаются асинхронно
- ✅ События для синхронизации загрузки
- ✅ Адаптивный дизайн для всех страниц
- ✅ Поддержка темной/светлой темы

---

**Создано:** Ноябрь 2025  
**Версия:** 1.0  
**Статус:** Готово для использования ✅
